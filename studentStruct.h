#pragma once
#include <iostream>
#include <vector>
#include <array>
#include <fstream>
#include <vector>

using namespace std;

//struct to manage the student data
struct studentDetails
{
	double grade;
	vector<char> fname;
	vector<char> lname;
	int year;
	int age;
	char role;
};